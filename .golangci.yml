run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gosec
    - bodyclose
    - errorlint
    - misspell
    - whitespace
    - unparam
    - nakedret
    - prealloc
    - errcheck

linters-settings:
  revive:
    confidence: 0.8
    rules:
      - name: exported
        disabled: true
      - name: package-comments
        disabled: true
  govet:
    enable-all: true
    disable:
      - fieldalignment
  staticcheck:
    checks: ["all"]

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - unparam
        - prealloc
        - errcheck
